<%= form_with(model: book_mark, local: true) do |form| %>
  <% if book_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_mark.errors.count, "error") %> prohibited this book_mark from being saved:</h2>

      <ul>
      <% book_mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :book_mark_title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :book_mark_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
